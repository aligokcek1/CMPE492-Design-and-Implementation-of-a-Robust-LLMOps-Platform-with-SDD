[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["data/", ".venv/", "__pycache__/"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "SLF"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["SLF001"]
